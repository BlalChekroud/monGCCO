{% extends 'base.html.twig' %}

{% block title %}{{'country.title'|trans }}{% endblock %}

{% block body %}
<main id="main" class="main">

    <div class="pagetitle">
      <h1>{{'country.title_show'|trans }}</h1>
      <nav>
        <ol class="breadcrumb">
          <li class="breadcrumb-item"><a href="{{ path('home') }}"><i class="ri-home-4-line"></i> {{ 'breadcrumb_home'|trans }}</a></li>
          <li class="breadcrumb-item"><a href="{{ path('app_country_index') }}">{{ 'country.manage'|trans }}</a></li>
          <li class="breadcrumb-item active">{{'show'|trans }}</li>
        </ol>
      </nav>
    </div><!-- End Page Title -->

    <section class="section">
        <div class="row">
            <div class="col-sm-12 col-md-12 col-lg-12 col-xl-12">
                <div class="card">
                    <div class="card-body">
                     <div id="printableArea">
                        <h5 class="card-title">{{'show'|trans }}</h5>
                        <table class="table">
                            <tbody>
                                <tr>
                                    <th>{{'country.name'|trans }}</th>
                                    <td>{{ country.name }}</td>
                                </tr>
                                <tr>
                                    <th>{{'country.iso'|trans }}</th>
                                    <td>{{ country.iso2 }}</td>
                                </tr>
                                <tr>
                                    <th>{{'regions'|trans }} :<h6 class="explanation mx-4">{{'cities'|trans }}</h6></th>
                                    <td>
                                        {% if country.regions|length > 0 %}
                                            {% set sorted_regions = country.regions|sort((a, b) => a.name <=> b.name) %}
                                            <ul>
                                                {% for region in sorted_regions %}
                                                    <li>
                                                        {{ region.name }}
                                                        {% if region.cities|length > 0 %}
                                                            {% set sorted_cities = region.cities|sort((a, b) => a.name <=> b.name) %}
                                                            <ul>
                                                                {% for city in sorted_cities %}
                                                                    <li>
                                                                        <i>{{ city.name}}</i>
                                                                    </li>
                                                                {% endfor %}
                                                            </ul>
                                                        {% else %}
                                                            <p>{{'city.none'|trans }}</p>
                                                        {% endif %}
                                                    </li>
                                                {% endfor %}
                                            </ul>
                                        {% else %}
                                            <p>{{'region.none'|trans }}</p>
                                        {% endif %}
                                    </td>
                                </tr>
                                <tr>
                                    <th>{{ 'created_at'|trans }}</th>
                                    <td>{{ country.createdAt ? country.createdAt|date('d-m-Y H:i:s') : '' }}</td>
                                </tr>
                                <tr>
                                    <th>{{ 'Last_updated_at'|trans }}</th>
                                    <td>{{ country.updatedAt ? country.updatedAt|date('d-m-Y H:i:s') : '' }}</td>
                                </tr>
                            </tbody>
                        </table>
                     </div>

                    </div>
                    <div class="card-footer d-flex justify-content-between">
                        <div>
                            <a href="{{ path('app_country_index') }}" class="btn btn-sm btn-outline-secondary mx-1" title="{{ 'country.return'|trans }}">{{ 'back'|trans }}</a>
                            <a href="{{ path('app_country_new') }}" class="btn btn-outline-info btn-sm bi bi-plus-circle-dotted"> {{'add_a_country'|trans }}</a>
                        </div>
                        <div class="d-flex justify-content-end">
                            {% import 'macros/btn_actions_macros.html.twig' as macros %}
                            {{ macros.action_buttons('app_country_new','app_country_show', 'app_country_edit', 'country/_delete_form.html.twig', country, app.request.attributes.get('_route')) }}
                            {{ include('country/_delete_form.html.twig') }}
                        </div>
                    </div>
                </div>

            </div>
        </div>
    </section>
{% endblock %}
