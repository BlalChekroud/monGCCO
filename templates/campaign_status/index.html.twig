{% extends 'base.html.twig' %}

{% block title %}Consultation des états de campagne{% endblock %}

{% block body %}
<main id="main" class="main">

    <div class="pagetitle">
        <h1>Consultation des états de campagne</h1>
        <nav>
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="{{ path('home') }}"><i class="ri-home-4-line"></i> Accueil</a></li>
                <li class="breadcrumb-item active">Gestion des états de campagne</li>
            </ol>
        </nav>
    </div><!-- End Page Title -->

    <section class="section">
        <div class="row">
            <div class="col-sm-12 col-md-12 col-lg-12 col-xl-12">
                <div class="card">
                    <div class="card-body">
                        <div class="d-flex justify-content-between">
                            <h5 class="card-title">Listes</h5>
                            <button type="button" class="btn btn-sm btn-info bi bi-book" data-bs-toggle="modal" data-bs-target="#largeModal"> Aide</button>
                            
                            <!-- Large Modal for AIDE -->
                            <div class="modal fade" id="largeModal" tabindex="-1">
                                <div class="modal-dialog modal-lg">
                                    <div class="modal-content">
                                        {% include("campaign_status/attention.html.twig") %}
                                        <div class="modal-footer">
                                            <button type="button" class="btn btn-sm btn-secondary bi bi-x-lg" data-bs-dismiss="modal" title="Fermer"></button>
                                        </div>
                                    </div>
                                </div>
                            </div>

                        </div>
                        <div id="printableArea">
                            <table class="table datatable">
                                <thead>
                                    <tr>
                                        <th>#</th>
                                        <th>Label</th>
                                        <th>Crée le</th>
                                        <th>Dernière mise à jour le</th>
                                        <th>Actions</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    {% for campaign_status in campaign_statuses %}
                                    <tr>
                                        <td>{{ campaign_status.id }}</td>
                                        <td>{{ campaign_status.label }}</td>
                                        <td>{{ campaign_status.createdAt ? campaign_status.createdAt|date('d-m-Y H:i:s') : '' }}</td>
                                        <td>{{ campaign_status.updatedAt ? campaign_status.updatedAt|date('d-m-Y H:i:s') : '' }}</td>
                                        <td>
                                            <div class="d-flex justify-content-center">
                                                {% import 'macros/btn_actions_macros.html.twig' as macros %}
                                                {{ macros.action_buttons('app_campaign_status_new', 'home', 'app_campaign_status_edit', 'campaign_status/_delete_form.html.twig', campaign_status, app.request.attributes.get('_route')) }}
                                                {{ include('campaign_status/_delete_form.html.twig') }}
                                            </div>
                                        </td>
                                    </tr>
                                    {% else %}
                                    <tr>
                                        <td colspan="5">Aucun enregistrement trouvé</td>
                                    </tr>
                                    {% endfor %}
                                </tbody>
                            </table>
                        </div>

                    </div>
                    <div class="card-footer">
                        <a href="{{ path('app_campaign_status_new') }}" class="btn btn-outline-info btn-sm bi bi-plus-circle-dotted"> Créer un état de campagne</a>
                        <a href="{{ path('app_counting_campaign_new') }}" class="btn btn-outline-info btn-sm bi bi-plus-circle-dotted mx-1"> Créer une campagne</a>
                        <a href="{{ path('app_counting_campaign_index') }}" class="btn btn-outline-info btn-sm bi bi-list-task mx-1"> Consultation des campagnes</a>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section class="section">
        <div class="row">
            <div class="col-lg-12">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">Instructions pour la création des états de campagne</h5>

                        <!-- Bouton pour ouvrir la popup -->
                        <button type="button" class="btn btn-warning bi bi-exclamation-triangle-fill" data-bs-toggle="modal" data-bs-target="#campaignStatusModal">
                            Voir les instructions
                        </button>

                    </div>
                </div>
            </div>
        </div>
    </section>
    
    <!-- Popup (fenêtre modale) -->
    <div class="modal fade" id="campaignStatusModal" tabindex="-1" aria-labelledby="campaignStatusModalLabel" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header bg-danger text-white">
                    <h5 class="modal-title" id="campaignStatusModalLabel">Attention: Ordre obligatoire des états de campagne</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <p>Voici l'ordre <strong>obligatoire</strong> pour créer les états de campagne. Vous devez ajouter tous les sept pour garantir que l'application fonctionne correctement.</p>
                    <ol>
                        <li><strong>Planifiée</strong></li>
                        <li><strong>En cours</strong></li>
                        <li><strong>Terminée</strong></li>
                        <li><strong>Clôturée</strong></li>
                        <li><strong>Erreur</strong></li>
                        <li><strong>Suspendue</strong></li>
                        <li><strong>Annulée</strong></li>
                    </ol>
                    <p>Vous pouvez modifier les noms, mais ces sept états doivent être présents et dans cet ordre, sans quoi des erreurs pourraient survenir dans le système.</p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Fermer</button>
                </div>
            </div>
        </div>
    </div>

</main><!-- End #main -->
{% endblock %}
