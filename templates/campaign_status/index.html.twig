{% extends 'base.html.twig' %}

{% block title %}CampaignStatus index{% endblock %}

{% block body %}
    <h1>CampaignStatus index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Label</th>
                <th>CreatedAt</th>
                <th>UpdatedAt</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for campaign_status in campaign_statuses %}
            <tr>
                <td>{{ campaign_status.id }}</td>
                <td>{{ campaign_status.label }}</td>
                <td>{{ campaign_status.createdAt ? campaign_status.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ campaign_status.updatedAt ? campaign_status.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('app_campaign_status_show', {'id': campaign_status.id}) }}">show</a>
                    <a href="{{ path('app_campaign_status_edit', {'id': campaign_status.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_campaign_status_new') }}">Create new</a>
{% endblock %}
