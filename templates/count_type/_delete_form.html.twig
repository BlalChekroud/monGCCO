<form method="post" action="{{ path('app_count_type_delete', {'id': count_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ count_type.id) }}">
    <button class="btn">Delete</button>
</form>
