<form method="post" action="{{ path('app_bird_family_delete', {'id': bird_family.id}) }}" onsubmit="Êtes-vous sûr de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bird_family.id) }}">
    <button class="btn btn-outline-danger btn-sm bi bi-x-circle-fill"> Supprimer</button>
</form>
