<form method="post" action="{{ path('app_bird_species_group_delete', {'id': bird_species_group.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bird_species_group.id) }}">
    <button class="btn">Delete</button>
</form>
