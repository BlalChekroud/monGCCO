{% if is_granted('ROLE_ADMIN') %}
<form method="post" action="{{ path('app_bird_life_tax_treat_delete', {'id': bird_life_tax_treat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bird_life_tax_treat.id) }}">
    <button class="btn">Delete</button>
</form>
{% endif %}
