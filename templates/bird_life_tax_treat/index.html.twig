{% extends 'base.html.twig' %}

{% block title %}{{'birdLifeTaxTreat.title'|trans }}{% endblock %}

{% block body %}
  <main id="main" class="main">

    <div class="pagetitle">
      <h1>{{'birdLifeTaxTreat.title'|trans }}</h1>
      <nav>
        <ol class="breadcrumb">
          <li class="breadcrumb-item"><a href="{{ path('home') }}"><i class="ri-home-4-line"></i> {{ 'breadcrumb_home'|trans }}</a></li>
          <li class="breadcrumb-item active">{{'birdLifeTaxTreat.manage'|trans }}</li>
        </ol>
      </nav>
    </div><!-- End Page Title -->

    <section class="section">
      <div class="row">
        <div class="col-sm-12 col-md-12 col-lg-12 col-xl-12">
          <div class="card">
            <div class="card-body">
              <h5 class="card-title">{{ 'card_title'|trans }}</h5>
                <table class="table datatable">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>{{'label'|trans }}</th>
                            <th>{{ 'created_at'|trans }}</th>
                            <th>{{ 'Last_updated_at'|trans }}</th>
                            <th>{{ 'actions'|trans }}</th>
                        </tr>
                    </thead>
                    <tbody>
                    {% for bird_life_tax_treat in bird_life_tax_treats %}
                        <tr>
                            <td>{{ bird_life_tax_treat.id }}</td>
                            <td>{{ bird_life_tax_treat.label }}</td>
                            <td>{{ bird_life_tax_treat.createdAt ? bird_life_tax_treat.createdAt|date('d-m-Y H:i:s') : '' }}</td>
                            <td>{{ bird_life_tax_treat.updatedAt ? bird_life_tax_treat.updatedAt|date('d-m-Y H:i:s') : '' }}</td>
                            <td>
                                <div class="d-flex justify-content-end">
                                    {% import 'macros/btn_actions_macros.html.twig' as macros %}
                                    {{ macros.action_buttons('app_bird_life_tax_treat_new','home', 'app_bird_life_tax_treat_edit', 'bird_life_tax_treat/_delete_form.html.twig', bird_life_tax_treat, app.request.attributes.get('_route'), false) }}
                                    {{ include('bird_life_tax_treat/_delete_form.html.twig') }}
                                </div>
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="5">{{ 'no_record_found'|trans }}</td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>        
              
            </div>
            <div class="card-footer">
              <a href="{{ path('app_bird_life_tax_treat_new') }}" class="btn btn-outline-info btn-sm bi bi-plus-circle-dotted"> {{'add_a_birdLifeTaxTreat'|trans }}</a>
              <a href="{{ path('app_collected_data_index') }}" class="btn btn-outline-info btn-sm bi bi-list-task mx-2"> {{ 'collect.consult'|trans }}</a>
            </div>
          </div>

        </div>
      </div>
    </section>

    {% import 'macros/form_macros.html.twig' as form_macros %}
    {{ form_macros.csv_import_form(form, 'app_bird_life_tax_treat_index', formExport, 'Import birdLifeTaxTreat Data', 'Export birdLifeTaxTreat Data') }}


    {% if headers is defined and rows is defined %}
        {{ form_macros.csv_modal(headers, rows, csvData, 'app_bird_life_tax_treat_index', ['TAX TREAT']) }}
    {% endif %}

  </main><!-- End #main -->
{% endblock %}