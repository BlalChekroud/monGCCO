{% extends 'base.html.twig' %}

{% block title %}BirdLifeTaxTreat index{% endblock %}

{% block body %}
    <h1>BirdLifeTaxTreat index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>#</th>
                <th>Label</th>
                <th>Crée le</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for bird_life_tax_treat in bird_life_tax_treats %}
            <tr>
                <td>{{ bird_life_tax_treat.id }}</td>
                <td>{{ bird_life_tax_treat.label }}</td>
                <td>{{ bird_life_tax_treat.createdAt ? bird_life_tax_treat.createdAt|date('d-m-Y H:i:s') : '' }}</td>
                <td>{{ bird_life_tax_treat.updatedAt ? bird_life_tax_treat.updatedAt|date('d-m-Y H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('app_bird_life_tax_treat_show', {'id': bird_life_tax_treat.id}) }}">show</a>
                    <a href="{{ path('app_bird_life_tax_treat_edit', {'id': bird_life_tax_treat.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_bird_life_tax_treat_new') }}">Create new</a>
{% endblock %}
