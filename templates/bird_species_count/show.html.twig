{% extends 'base.html.twig' %}

{% block title %}BirdSpeciesCount{% endblock %}

{% block body %}
    <h1>BirdSpeciesCount</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bird_species_count.id }}</td>
            </tr>
            <tr>
                <th>Count</th>
                <td>{{ bird_species_count.count }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_bird_species_count_index') }}">back to list</a>

    <a href="{{ path('app_bird_species_count_edit', {'id': bird_species_count.id}) }}">edit</a>

    {{ include('bird_species_count/_delete_form.html.twig') }}
{% endblock %}
