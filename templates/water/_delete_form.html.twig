{% if is_granted('ROLE_DELETE') %}
    <form method="post" action="{{ path('app_water_delete', {'id': water.id}) }}" onsubmit="return confirm('{{ 'delete_confirm'|trans }}');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ water.id) }}">
        <button class="btn btn-danger btn-sm bi bi-trash-fill mx-2" title="{{ 'conditionWater.delete'|trans }}"></button>
    </form>
{% endif %}