<form method="post" action="{{ path('app_water_delete', {'id': water.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ water.id) }}">
    <button class="btn">Delete</button>
</form>
