{{ form_start(form) }}
    {{ form_widget(form) }}
    {% import 'macros/btn_actions_macros.html.twig' as macros %}
      {{ macros.action_buttons('app_image_new', 'home', 'app_image_edit', 'image/_delete_form.html.twig', image, app.request.attributes.get('_route')) }}
{{ form_end(form) }}
