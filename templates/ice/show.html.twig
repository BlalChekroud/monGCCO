{% extends 'base.html.twig' %}

{% block title %}Ice{% endblock %}

{% block body %}
    <h1>Ice</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ice.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ ice.label }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ ice.createdAt ? ice.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ ice.updatedAt ? ice.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ice_index') }}">back to list</a>

    <a href="{{ path('app_ice_edit', {'id': ice.id}) }}">edit</a>

    {{ include('ice/_delete_form.html.twig') }}
{% endblock %}
