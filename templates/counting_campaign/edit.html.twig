{% extends 'base.html.twig' %}

{% block title %}Modification de la campagne de comptage{% endblock %}

{% block body %}

<main id="main" class="main">

    <div class="pagetitle">
      <h1>Modification de la campagne</h1>
      <nav>
        <ol class="breadcrumb">
          <li class="breadcrumb-item"><a href="{{ path('home') }}">Accueil</a></li>
          <li class="breadcrumb-item"><a href="{{ path('app_counting_campaign_index') }}">Gestion des campagnes</a></li>
          <li class="breadcrumb-item active">Modification</li>
        </ol>
      </nav>
    </div><!-- End Page Title -->

    <section class="section">
        <div class="row">
            <div class="col-lg-10">

                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">
                            Modifier
                        </h5>

                        {{ include('counting_campaign/_form.html.twig', {'button_label': ' Mise à jour'}) }}<br/>
                        
                        <div class="row">
                            <div class="col-lg-4">
                                <a href="{{ path('app_counting_campaign_index') }}" class="btn btn-outline-secondary btn-sm bi bi-arrow-return-left"> retour vers la liste</a>
                            </div>
                            <div class="col-lg-3">
                                {{ include('counting_campaign/_delete_form.html.twig') }}
                            </div>
                            <div class="col-lg-5">
                                <a href="{{ path('app_site_collection_new') }}" class="btn btn-outline-info btn-sm bi bi-plus-circle-fill">
                                    Créer nouvelle collection de sites
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
</main>
{% endblock %}
