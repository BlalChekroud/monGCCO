{{ form_start(form) }}
    {# {{ form_widget(form) }} #}
    <div class="row mb-3">
      <div class="col-sm-3 col-lg-3 my-3">
        {{ form_label(form.startDate, 'Date de début') }}
        {{ form_widget(form.startDate, {'attr': {'class': 'form-control'}}) }}
      </div>
      <div class="col-sm-3 col-lg-3 my-3 mx-3">
        {{ form_label(form.endDate, 'Date de fin') }}
        {{ form_widget(form.endDate, {'attr': {'class': 'form-control'}}) }}
      </div>
      <div class="col-sm-4 col-lg-4 my-3 mx-3">
        {{ form_label(form.campaignStatus, 'Statut de la campagne') }}
        {{ form_widget(form.campaignStatus, {'attr': {'class': 'form-control'}}) }}
      </div>
    </div>
    {{form_rest(form) }}
    <div class="row mb-3">
      <div class="col-sm-4 col-lg-4">
        <button type="reset" class="btn btn-outline-dark bi bi-x-square"> Annuler</button>
      </div>
      <div class="col-sm-5 col-lg-5">
        <button class="btn btn-outline-primary bi bi-save"> {{ button_label|default('Enregistrer') }}</button>
      </div>
    </div>
{{ form_end(form) }}
