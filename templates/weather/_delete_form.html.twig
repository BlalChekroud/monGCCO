{% if is_granted('ROLE_DELETE') %}
    <form method="post" action="{{ path('app_weather_delete', {'id': weather.id}) }}" onsubmit="return confirm('{{ 'delete_confirm'|trans }}');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ weather.id) }}">
        <button class="btn btn-danger btn-sm bi bi-trash-fill mx-2" title="{{ 'conditionWeather.delete'|trans }}"></button>
    </form>
{% endif %}