<form method="post" action="{{ path('app_environmental_conditions_delete', {'id': environmental_condition.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ environmental_condition.id) }}">
    <button class="btn btn-danger btn-sm bi bi-trash mx-2" title="Supprimer"></button>
</form>
