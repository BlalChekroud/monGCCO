{% extends 'base.html.twig' %}

{% block title %}EnvironmentalConditions{% endblock %}

{% block body %}
    <h1>EnvironmentalConditions</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ environmental_condition.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ environmental_condition.name }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ environmental_condition.createdAt ? environmental_condition.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ environmental_condition.updatedAt ? environmental_condition.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_environmental_conditions_index') }}">back to list</a>

    <a href="{{ path('app_environmental_conditions_edit', {'id': environmental_condition.id}) }}">edit</a>

    {{ include('environmental_conditions/_delete_form.html.twig') }}
{% endblock %}
