<form method="post" action="{{ path('app_tidal_delete', {'id': tidal.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tidal.id) }}">
    <button class="btn">Delete</button>
</form>
