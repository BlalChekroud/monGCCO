{% extends 'base.html.twig' %}

{% block title %}Method index{% endblock %}

{% block body %}
    <h1>Method index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Label</th>
                <th>CreatedAt</th>
                <th>UpdatedAt</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for method in methods %}
            <tr>
                <td>{{ method.id }}</td>
                <td>{{ method.label }}</td>
                <td>{{ method.createdAt ? method.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ method.updatedAt ? method.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('app_method_show', {'id': method.id}) }}">show</a>
                    <a href="{{ path('app_method_edit', {'id': method.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_method_new') }}">Create new</a>
{% endblock %}
