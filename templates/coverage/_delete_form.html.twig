{% if is_granted('ROLE_DELETE') %}
    <form method="post" action="{{ path('app_coverage_delete', {'id': coverage.id}) }}" onsubmit="return confirm('{{ 'delete_confirm'|trans }}');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ coverage.id) }}">
        <button class="btn btn-danger btn-sm bi bi-trash-fill mx-2" title="{{ 'coverage.delete'|trans }}"></button>
    </form>
{% endif %}